// Create a new div element
const newElement = document.createElement('div');

// Set HTML content for the new element
newElement.innerHTML = '<h1 class="FirstDemo">Hello People</h1><button id="click-me-id">Click me</button>';

// Append the new element to an existing element (e.g., the body)
document.body.appendChild(newElement);